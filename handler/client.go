package handler

import (
	"net/http"
)

// ClientServer is used when Developer Org manages their own clients
func ClientServer(w http.ResponseWriter, r *http.Request) {
	//client := r.URL.Path[len("/clients/"):]
	//fmt.Fprint(w, "success")
}

//func GetClientSecret
